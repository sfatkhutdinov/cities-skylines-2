[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    mock: marks tests that use mock environment
addopts = --strict-markers -v
mock_env = False 